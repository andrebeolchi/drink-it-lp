// Placeholder for a future web ad slot (e.g. Google AdSense/Ad Manager).
// The mobile app shows AdMob banners/interstitials; there's no web ad
// account configured yet, so this renders nothing until one is wired up.
// Keeping the component (instead of removing ad call sites) means turning
// ads on later is a one-file change.
export function AppAdSlot() {
  return null
}
